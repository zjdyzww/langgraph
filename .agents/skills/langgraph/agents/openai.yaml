interface:
  display_name: "Langgraph"
  short_description: "Repo-specific patterns and workflows for langgraph"
  default_prompt: "Use the langgraph repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true